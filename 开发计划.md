# 巡检机器人远程控制系统 — 开发计划

> 版本：V2.0（2026-09-03 翻新，反映 WSL2 仿真线实际进展）
> 配套文档：《巡检机器人远程控制方案（蒲公英组网版）.md》《WSL2仿真环境交接文档.md》
> 核心约束：**开发机为 Jetson（ARM64），仿真/主力开发在 WSL2（x64），量产主机为 i7 工控机（x86_64）**，全部代码必须零改动移植，仅允许改配置。
> **交付节点：10 月 20 日（北京信息科技大学项目，2 台 JK-HS606-XJ）**

---

## 一、可移植性设计原则（贯穿全程的铁律）

| # | 原则 | 说明 |
|---|---|---|
| 1 | 语言与框架天然跨架构 | Python3 + ROS2 Humble 标准包 + 浏览器前端，两端均有官方支持（Ubuntu 22.04 arm64/amd64） |
| 2 | 二进制依赖按架构下载 | MediaMTX 等预编译组件按 `uname -m` 拉取（arm64 在 Jetson，amd64 在 WSL2/工控机） |
| 3 | 视频编码器抽象 | `config/config.yaml` 中配置；Jetson 用硬编，x64 用 `h264_vaapi`，兜底 `libx264` |
| 4 | 推理框架以 ONNX Runtime CPU 为基线 | 两端都能跑；TensorRT / OpenVINO 作为后期可选加速，不进主干代码 |
| 5 | 禁用平台专有 API | 不用 DeepStream 等 Jetson 专有库；底盘硬件接口统一收口在驱动层（Ecu/CAN） |
| 6 | 配置与代码分离 | IP、端口、编码器、速度上限等全部外置 `config/config.yaml`，换机只改配置 |
| 7 | 真车/仿真同接口 | 网页/后台/巡逻逻辑只认话题与坐标系（`/cmd_vel`、`/odom`、`/scan`、map 系），仿真验证过的代码真车零改动平移 |

---

## 二、目录结构约定（当前实际）

```
Remote_ctrol/
├── 巡检机器人远程控制方案（蒲公英组网版）.md   # 方案
├── 开发计划.md                                # 本文件
├── WSL2仿真环境交接文档.md                     # 仿真环境与踩坑记录
├── 智能巡检机器人项目.docx                     # 合同技术参数
├── config/config.yaml                         # 全部可调参数
├── scripts/                                   # install_sim.sh / 推流 / 一键启动
│   ├── start_m1.sh                            #   M1 切片启动（Jetson）
│   ├── start_sim_monitor.sh                   #   仿真+监控台（无 Nav2）
│   ├── start_nav2_monitor.sh                  #   Nav2 全栈+监控台（当前主用）
│   ├── push_camera.sh / push_test_video.sh    #   摄像头/测试源推流
│   └── push_d435_wsl.sh                       #   WSL2 D435(usbipd) 推流
├── deploy/mediamtx.yml                        # 流媒体配置
├── bin/mediamtx                               # 流媒体二进制（按架构）
├── web/
│   ├── index.html                             # M1 遥控页（视频+摇杆）
│   ├── monitor.html                           # 远程监控台（地图/雷达/位姿/视频/遥控/初始定位/目标点）
│   └── maps/                                  # 地图 PNG + meta.json（供页面）
└── ros2_ws/src/
    ├── robot_gateway/                         # WebSocket 网关：遥控指令/遥测/初始定位/Nav2 action client
    ├── chassis_description/                   # 车模 URDF（厂家 URDF 移植修正版）
    ├── chassis_gazebo/                        # Gazebo 仿真（阿克曼插件 + 双雷达 + 场地）
    ├── chassis_nav2/                          # Nav2 接入（nav2_sim.yaml / BT / 地图 / launch）
    ├── JD03/ hi_driver/ rslidar_sdk-v1.5.19/  # 厂家驱动包（已编译备真车）
    ├── wit_ros2_imu/ imu_msg/ hk_camera/ HK/  # 厂家驱动包（已编译备真车）
    └── ocr_interfaces/ ocr_node/              # 数字仪表识别（自 kybot 迁入）
```

---

## 三、里程碑与状态

### M0 — 开发环境就绪 ✅ 已完成
- Jetson：ffmpeg、Python 依赖、MediaMTX（arm64）
- WSL2：ROS2 Humble、Gazebo 11、Nav2 全家桶（`scripts/install_sim.sh`）

### M1 — 视频 + 遥控垂直切片 ✅ 已完成
- MediaMTX（RTSP 8554 / WebRTC 8889）+ FFmpeg 推流
- `robot_gateway`：WebSocket(8765) → `/cmd_vel`，1s 心跳失控刹车，速度限幅
- `web/index.html`：视频 + 虚拟摇杆 + WASD + 急停
- 局域网/蒲公英组网实测通过
- **后续扩展（已完成）**：网关增加遥测推送（位姿/雷达点/速度/导航状态）、`/initialpose` 发布、Nav2 action client（目标点下发/取消）、闲置静默（不踩 Nav2 的 /cmd_vel）

### M2 — 业务后端 ⬜ 未开始（下一步重点之一）
- FastAPI + SQLite：巡检点/路线/计划 CRUD、图片管理、识别记录、报告生成（HTML）
- 任务调度器：计划本机缓存执行（断网可跑的底线）
- `web/` 升级为后端托管的后台页面框架（点位标注、计划配置）
- 验收：后台配置一条 3 点巡检计划并入库；到点自动触发

### M3 — 自主巡逻 🔶 进行中（仿真线已完成约 70%）
**已完成（全部在 WSL2 仿真验证）：**
- 阿克曼底盘 Gazebo 仿真（`chassis_description` + `chassis_gazebo`，双雷达：16 线点云 + 2D `/scan`）
- slam_toolbox 建图存图（`chassis_nav2/maps/patrol.*`）
- **Nav2 接入**（`chassis_nav2`）：AMCL 定位（行驶误差 0.02~0.13m，静置不发散）+ Smac Hybrid-A* 规划 + RPP 控制 + 自定义 BT（去 Spin）+ 到点即判完成
- **远程监控台**（`web/monitor.html`）：地图/雷达点/位姿朝向 + D435 真实视频（usbipd 透传，按需开关）+ 仪表 + 摇杆遥控 + **初始定位点选** + **目标点下发/取消** + 导航状态显示，自适应 1080p/2K
- 一键启动：`scripts/start_nav2_monitor.sh`

**待做：**
- 导航速度调优（当前 ~0.12 m/s 偏慢：插件轮速 PID 加 I 项 + 平滑器参数放开）
- `patrol_manager`：计划加载 → Waypoint Follower → 点位动作（停留/抓拍/播报）→ 任务记录（M3 核心产出）
- 一键返回（回充电桩点）、一键到达（点位化）
- twist_mux 仲裁（手持 > 远程 > 导航，现为网关闲置静默过渡方案）
- Twist→Ecu 适配节点（阿克曼运动学换算，仿真先验证数值，真车接上即用）
- 验收：仿真里定时触发 3 点路线，无人值守跑完并生成任务记录

### M4 — 音频与识别 ⬜ 未开始
- 对讲：下行随视频走 MediaMTX；上行 WHIP 推流。**浏览器麦克风要求 HTTPS**——mkcert 自签证书
- TTS：edge-tts（在线）+ 离线引擎兜底；`audio_player_node` / `tts_alert_node`
- `capture_server_node`：抓拍存档（含磁盘轮转清理策略）
- `detector_node`：YOLOv8（ONNX Runtime）人脸/行人，车牌二期
- 数字仪表识别：`ocr_node` 已有基础（合同点名项）
- 验收：双向对讲清晰；识别到目标自动抓拍并上报后台

### M5 — 3D 导航 + 移植联调 ⬜ 未开始
- **室外 3D PCD 导航**（合同点名）：复用 kybot 的 `FAST_LIO_LOCALIZATION_ROS2`（x64 重编 + AVX2，i7 算力足够）；仿真先调链路（需加 IMU 插件），真车数据精调外参；robot_localization EKF 融合（ODOM+IMU+雷达，合同点名）
- i7 工控机装系统 → 按架构装依赖 → 改编码器配置（预计半天）
- 真车联调：Ecu/CAN 底盘、四传感器真机（速腾 16 线 / hi_driver / 维特 IMU / 海康云台）、`battery_status` 换掉模拟电池
- 组网按实测结论定稿：电信/联通卡 P2P 直连 / 蒲公英付费 / VPS+WireGuard
- 按方案第七章全量场测（含断网韧性）
- 验收：真实小车全功能场测通过

---

## 四、风险登记（持续跟踪）

| 风险 | 状态 | 缓解 |
|---|---|---|
| **厂家室外 3D PCD 导航源码交付**（合同点名） | ⚠️ 待催办 | 已有自主替代方案（kybot FAST_LIO 栈复用），两手准备 |
| **真车未到货** | 等待硬件 | 仿真线已覆盖导航/巡逻/后台大部分开发；真车仅需联调 |
| 组网方案未定（移动卡打洞失败） | 待电信/联通卡复测 | 代码与网络解耦；三条退路已明确 |
| 厂家待办：气体模块 Modbus 寄存器表、后轮转向内部逻辑（蟹行支持） | 待厂家回复 | 见交接文档第一节待办清单 |
| 对讲依赖 HTTPS 安全上下文 | 已知 | M4 用 mkcert 自签证书 |
| i7 无独显，识别帧率有限 | 可接受 | YOLOv8n/s + ONNX Runtime 约 10–15fps 够用；后期 OpenVINO 提速 |

---

## 五、当前状态（2026-09-03）

- [x] 方案评审完成，可行性确认
- [x] 蒲公英组网连通（Jetson 虚拟 IP 172.16.2.198）；移动卡 4G 打洞待电信/联通复测
- [x] M0 环境就绪（Jetson + WSL2）
- [x] M1 垂直切片（局域网/蒲公英实测通过）
- [x] 仿真底座：阿克曼底盘 Gazebo 仿真（车模修正/传感器/场地）
- [x] slam_toolbox 建图存图
- [x] Nav2 接入：AMCL + Hybrid-A* + RPP 全链仿真验证通过
- [x] 远程监控台（monitor.html）：地图/雷达/位姿/视频/遥控/初始定位/目标点
- [x] D435 真实视频链（usbipd → ffmpeg → MediaMTX → 网页按需播放）
- [ ] M2 业务后端
- [ ] M3 剩余：速度调优、patrol_manager、一键返回、twist_mux、Twist→Ecu
- [ ] M4 音频与识别
- [ ] M5 3D 导航 + 真车联调

## 六、下一步行动（按序）

1. 导航速度调优（插件轮速 PID 加 I 项 + 平滑器参数放开）
2. `patrol_manager` + 一键返回/到达（仿真验收：3 点无人值守 + 任务记录）
3. M2 业务后端（FastAPI + SQLite + 报告生成）
4. 并行催办：厂家导航源码、气体寄存器表、后轮转向逻辑、SIM 卡复测
