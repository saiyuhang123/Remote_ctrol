# 巡检机器人远程控制系统 — 开发计划

> 版本：V1.0
> 配套文档：《巡检机器人远程控制方案（蒲公英组网版）.md》
> 核心约束：**开发机为 Jetson（ARM64），量产主机为 i7-10750H 工控机（x86_64）**，全部代码必须零改动移植，仅允许改配置。

---

## 一、可移植性设计原则（贯穿全程的铁律）

| # | 原则 | 说明 |
|---|---|---|
| 1 | 语言与框架天然跨架构 | Python3 + ROS2 Humble 标准包 + 浏览器前端，两端均有官方支持（Ubuntu 22.04 arm64/amd64） |
| 2 | 二进制依赖按架构下载 | MediaMTX 等预编译组件统一由 `scripts/install.sh` 按 `uname -m` 拉取对应架构版本 |
| 3 | 视频编码器抽象 | `config/config.yaml` 中配置；Jetson 用 `nvv4l2h264enc`（硬编），x64 用 `h264_vaapi`（i7-10750H 核显 QSV），兜底 `libx264`（软编） |
| 4 | 推理框架以 ONNX Runtime CPU 为基线 | 两端都能跑；TensorRT（Jetson）/ OpenVINO（Intel 核显）作为后期可选加速，不进主干代码 |
| 5 | 禁用平台专有 API | 不用 DeepStream、jetson_utils 等 Jetson 专有库；GPIO/PWM 等底盘硬件接口待真实小车定版后统一收口在驱动层 |
| 6 | 配置与代码分离 | IP、端口、编码器、速度上限等全部外置 `config/config.yaml`，换机只改配置 |
| 7 | 依赖钉版本 | `requirements.txt` + `rosdep` + `install.sh`，保证两端环境一致 |

**移植到 i7 工控机时的全部工作**（预计半天）：
1. 装 Ubuntu 22.04 + ROS2 Humble（amd64 官方 deb）；
2. 拉代码，跑 `scripts/install.sh`（自动识别 amd64）；
3. `config.yaml` 里编码器改为 `h264_vaapi`；
4. 组网方案按届时测试结果定稿（蒲公英付费 / VPS）。

---

## 二、目录结构约定

```
Remote_ctrol/
├── 巡检机器人远程控制方案（蒲公英组网版）.md   # 方案（现有）
├── 开发计划.md                                # 本文件
├── config/config.yaml                         # 全部可调参数
├── scripts/                                   # install.sh / 推流 / 启动脚本
├── deploy/mediamtx.yml                        # 流媒体配置
├── src/                                       # ROS2 包（gateway、patrol、audio、detector…）
├── backend/                                   # FastAPI 业务后端 + SQLite
└── web/                                       # 管理后台前端（纯静态，无构建依赖起步）
```

---

## 三、里程碑

### M0 — 开发环境就绪（本周，0.5 天）
- 安装 ffmpeg、Python 依赖、MediaMTX（arm64 版）
- 验收：`install.sh` 一键跑通，产出环境检查报告

### M1 — 视频 + 遥控垂直切片（本周，1–2 天）
目标：浏览器看到测试画面，虚拟摇杆能驱动 `/cmd_vel`
- `deploy/mediamtx.yml` + MediaMTX 运行（RTSP 8554 / WebRTC 8889）
- `scripts/push_video.sh`：无摄像头阶段用 FFmpeg 测试源推流
- `src/robot_gateway/`：WebSocket(8765) → `/cmd_vel`，1s 心跳失控刹车，速度限幅
- `web/index.html`：WHEP 视频播放 + 虚拟摇杆 + 键盘控制
- 验收（局域网内，对应方案第七章 2/3 阶段）：
  - 电脑浏览器打开 `http://<板子IP>:8888` 看视频，延迟 < 500ms
  - 摇杆指令 → `ros2 topic echo /cmd_vel` 可见，响应 < 300ms
  - 松开摇杆/断开网页 1s 后自动零速度

### M2 — 业务后端（第 2 周）
- FastAPI + SQLite：巡检点/路线/计划 CRUD、图片管理、识别记录、报告生成（HTML）
- 任务调度器：计划本机缓存执行（断网可跑的底线）
- `web/` 升级为后端托管的后台页面框架
- 验收：后台配置一条 3 点巡检计划并入库；到点自动触发（先用模拟执行器）

### M3 — 自主巡逻（第 3–4 周）
- ROS2 包：`patrol_manager_node`、`twist_mux` 仲裁配置（手持 > 远程 > 导航）
- Nav2 + Gazebo 仿真差速小车：Waypoint Follower + 点位动作序列（停留/抓拍/播报）
- 一键返回、一键到达接口
- 验收（仿真环境）：3 点路线定时无人值守跑完，生成巡检报告

### M4 — 音频与识别（第 4–5 周）
- 对讲：下行随视频走 MediaMTX；上行 WHIP 推流。**注意浏览器麦克风要求 HTTPS**——用 mkcert 自签证书解决
- TTS：edge-tts（在线）+ 离线引擎兜底；`audio_player_node` / `tts_alert_node`
- `capture_server_node`：抓拍存档（含磁盘轮转清理策略）
- `detector_node`：YOLOv8（ONNX Runtime）人脸/行人，车牌二期
- 验收：双向对讲清晰；识别到目标自动抓拍并上报后台

### M5 — 移植与组网联调（工控机与网络方案到位后）
- i7 工控机装系统 → `install.sh` → 改编码器配置（预计半天）
- 组网按实测结论定稿：电信/联通卡 P2P 直连 / 蒲公英付费 / VPS+WireGuard
- 按方案第七章全量场测（含断网韧性）
- 验收：真实小车全功能场测通过

---

## 四、风险登记（持续跟踪）

| 风险 | 状态 | 缓解 |
|---|---|---|
| 组网方案未定（移动卡打洞失败） | 待电信/联通卡复测 | 代码与网络解耦，不影响开发；三条退路已明确 |
| 对讲依赖 HTTPS 安全上下文 | 已知 | M4 用 mkcert 自签证书 |
| i7 无独显，识别帧率有限 | 可接受 | YOLOv8n/s + ONNX Runtime 约 10–15fps 够用；后期 OpenVINO 提速 |
| 真实底盘驱动未介入 | 等待硬件 | 全程只用 `/cmd_vel` 抽象接口，底盘 ROS 驱动由小车厂提供，集成期对接 |
| 开发机无摄像头 | 已绕过 | M1–M3 用 FFmpeg 测试源；摄像头到货即插即用 |

---

## 五、当前状态

- [x] 方案评审完成，可行性确认
- [x] 蒲公英 ARM64 客户端安装、组网连通（虚拟 IP 172.16.2.198）
- [x] 移动卡 4G 打洞测试：转发模式（双层 NAT 假阴性可能，待电信/联通单层复测）
- [ ] M0 环境就绪
- [ ] M1 垂直切片
