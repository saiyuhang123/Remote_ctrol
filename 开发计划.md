# 巡检机器人远程控制系统 — 开发计划

> 版本：V3.0（2026-09-04 翻新，M2 完成后的全景）
> 配套文档：《巡检机器人远程控制方案（蒲公英组网版）.md》《WSL2仿真环境交接文档.md》
> 核心约束：**开发机 Jetson（ARM64）/ 仿真主力 WSL2（x64）/ 量产 i7 工控机（x86_64）**，代码零改动移植，仅改配置。
> **交付节点：10 月 20 日（北京信息科技大学项目，2 台 JK-HS606-XJ）**

---

## 一、可移植性设计原则（贯穿全程的铁律）

| # | 原则 | 说明 |
|---|---|---|
| 1 | 语言与框架天然跨架构 | Python3 + ROS2 Humble 标准包 + 浏览器前端 |
| 2 | 二进制依赖按架构下载 | MediaMTX 等按 `uname -m` 拉取 |
| 3 | 视频编码器抽象 | `config/config.yaml` 配置；Jetson 硬编 / x64 QSV / 兜底 libx264 |
| 4 | 推理框架以 ONNX Runtime CPU 为基线 | OpenVINO（i7 核显）为后期加速项，不进主干 |
| 5 | 禁用平台专有 API | 底盘硬件接口统一收口在驱动层（Ecu/CAN） |
| 6 | 配置与代码分离 | IP、端口、编码器、速度上限等外置 `config/config.yaml` |
| 7 | 真车/仿真同接口 | 上层只认话题与坐标系（`/cmd_vel`、`/odom`、`/scan`、map 系），仿真验证过的代码真车零改动平移 |
| 8 | 原始数据不出机器人 | 点云/图像在机端加工，远程只传加工品（遥测 JSON / H.264 视频） |

---

## 二、目录结构约定（当前实际）

```
Remote_ctrol/
├── 文档×4（方案/计划/交接/合同docx）
├── config/config.yaml              # 全局配置（控制限幅/端口/后端地址/视频编码器）
├── scripts/                        # install_sim / 一键启动×3 / 推流×3 / D435修复 / 建图巡航
├── deploy/mediamtx.yml             # 流媒体配置
├── bin/mediamtx                    # 流媒体二进制（按架构）
├── backend/                        # FastAPI 业务后端（M2）
│   ├── main.py                     #   入口：静态页面 + API 挂载 + 调度器启动
│   ├── db.py                       #   SQLite 六张表
│   ├── api_records.py              #   识别/抓拍记录
│   ├── api_points.py               #   巡检点 CRUD
│   ├── api_routes.py               #   路线 CRUD
│   ├── api_plans.py                #   计划 CRUD + 立即执行 + 任务记录
│   ├── scheduler.py                #   调度器（断网可跑）+ 模拟执行器
│   └── report.py                   #   巡检报告生成（HTML）
├── web/
│   ├── monitor.html                # 监控台（地图/雷达/位姿/视频/遥控/初始定位/目标点/告警）
│   ├── points.html                 # 巡检点标注页（地图拖箭头标点）
│   ├── plans.html                  # 路线/计划配置 + 任务记录 + 报告入口
│   ├── records.html                # 识别/抓拍记录检索
│   ├── index.html                  # M1 遥控页（存档）
│   ├── maps/                       # 地图 PNG + meta.json
│   ├── captures/                   # 抓拍存档（不入库）
│   └── reports/                    # 生成的巡检报告（不入库）
├── face_db/                        # 人脸底库（不入库）
├── data/                           # SQLite 数据库（不入库）
└── ros2_ws/src/
    ├── robot_gateway/              # WebSocket 网关（遥控/遥测/初始定位/导航目标/事件透传）
    ├── robot_common/               # HttpReporter（HTTP 上报 + 断网缓存补传）
    ├── detector_node/              # 图像识别（YOLOv8n+OpenVINO/YuNet+SFace/HyperLPR3）
    ├── capture_server/             # 抓拍服务（手动/自动/磁盘轮转）
    ├── chassis_description/        # 车模 URDF（厂家修正版）
    ├── chassis_gazebo/             # Gazebo 仿真（阿克曼+双雷达+场地）
    ├── chassis_nav2/               # Nav2 接入（建图/定位/规划/控制/BT/地图）
    ├── JD03/ hi_driver/ rslidar_sdk-v1.5.19/ wit_ros2_imu/ hk_camera/ HK/ imu_msg/   # 厂家驱动包（已编译备真车）
    └── ocr_interfaces/ ocr_node/   # 数字仪表识别（自 kybot 迁入）
```

---

## 三、里程碑与状态

### M0 — 开发环境就绪 ✅
Jetson + WSL2 双环境（ROS2 Humble / Gazebo / Nav2 / MediaMTX / Python 依赖）

### M1 — 视频 + 遥控垂直切片 ✅
视频链（RTSP→WebRTC）、遥控链（摇杆→/cmd_vel，1s 失控刹车），局域网/蒲公英实测通过

### M2 — 业务后端 ✅（2026-09-04 完成）
- FastAPI + SQLite：识别/抓拍入库、巡检点/路线/计划 CRUD、任务记录、巡检报告（HTML 自动生成）
- 调度器：固定时刻/每天/周期三种触发，本机执行断网可跑；模拟执行器验收"3 点计划自动触发+任务记录+报告"
- 页面：点位标注（地图拖箭头）、路线计划配置、记录检索、报告查看，五页互通
- 识别/抓拍直报后端（HttpReporter + SQLite 断网缓存补传）

### M3 — 自主巡逻 🔶（仿真链通，真车执行器等硬件）
**已完成（仿真验证）**：阿克曼 Gazebo 仿真、建图、Nav2 全链（AMCL+Hybrid-A*+RPP）、监控台（遥控/初始定位/目标点下发/导航状态）
**待做**：
- patrol_manager：模拟执行器 → 真实导航执行（Waypoint Follower + 点位动作）
- 一键返回（充电桩点）、twist_mux 仲裁（手持>远程>导航，现为网关闲置静默过渡）
- Twist→Ecu 适配节点（仿真先验证数值）
- 导航速度调优（当前 ~0.12 m/s 偏慢）
- AMCL 长程发散根治（IMU 仿真插件 + robot_localization EKF，兼治合同"多传感器融合定位"）
- 验收：真车/仿真 3 点路线无人值守跑完 + 巡检报告

### M4 — 音频与识别 🔶（识别已完成，音频待做）
**已完成**：detector_node（行人/人脸/车牌，检测+识别+上报+联动抓拍，跟踪稳定化+按人聚合去重）、capture_server（手动/自动抓拍+磁盘轮转）、D435 真实视频链（usbipd）
**待做**：
- TTS 播报/智能提示（edge-tts 在线 + 离线兜底；audio_player_node / tts_alert_node）
- 双向对讲（浏览器麦克风需 HTTPS——mkcert 自签；开发期 localhost 豁免）
- 数字仪表识别接入（ocr_node，合同点名）
- 人脸识别底库录入页（face_db 管理）
- 验收：双向对讲清晰；识别到目标自动抓拍并上报后台 ✅（识别侧已达成）

### M5 — 3D 导航 + 移植联调 ⬜（等真车）
- 室外 3D PCD 导航（合同点名）：复用 kybot `FAST_LIO_LOCALIZATION_ROS2`（x64 重编 + AVX2）；仿真先调链路（需加 IMU 插件），真车数据精调外参；robot_localization EKF 融合
- i7 工控机部署（半天）、真车联调（Ecu/CAN、四传感器真机、battery_status 换真）
- 组网定稿（电信/联通复测 / 蒲公英付费 / VPS）、断网韧性、全量场测
- Cartographer/TEB 作为合同点名"内置算法"装配可切换（各约 1 天）
- 验收：真实小车全功能场测通过

---

## 四、风险登记（持续跟踪）

| 风险 | 状态 | 缓解 |
|---|---|---|
| **厂家室外 3D PCD 导航源码交付**（合同点名） | ⚠️ 待催办 | 自主替代方案在手（kybot FAST_LIO 栈） |
| **真车未到货** | 等待硬件 | 仿真+后台已覆盖大部分开发；真车仅需联调 |
| AMCL 长程发散（仿真 bare 场地） | 已定性 | 调度 IMU+EKF 根治；真车环境特征丰富+IMU 不在此列 |
| 组网方案未定 | 待电信/联通卡复测 | 代码与网络解耦；三条退路 |
| 厂家待办：气体模块寄存器表、后轮转向逻辑（蟹行） | 待厂家回复 | 见交接文档第一节 |
| 对讲需 HTTPS | 已知 | mkcert 自签；开发期 localhost 豁免 |
| i7 无独显识别帧率 | 已验证可行 | OpenVINO+抽帧+限线程；预留 OpenVINO iGPU/量化提速 |

---

## 五、当前状态（2026-09-04）

- [x] M0 环境 / M1 垂直切片（局域网+蒲公英实测）
- [x] 仿真底座：阿克曼底盘 + 双雷达 + 巡逻场地
- [x] 建图 + Nav2 接入（AMCL/Hybrid-A*/RPP，短程导航成功）
- [x] 远程监控台（五页：监控台/点位/路线计划/记录/报告）
- [x] D435 真实视频链（usbipd 透传 + 按需开关）
- [x] M2 业务后端（入库/CRUD/调度/模拟执行/报告）
- [x] M4 识别一期（行人/人脸/车牌 + 联动抓拍 + 去重上报）
- [ ] M3 剩余：patrol_manager 真执行、一键返回、twist_mux、Twist→Ecu、调速、EKF
- [ ] M4 剩余：TTS、对讲、仪表识别接入、人脸底库管理页
- [ ] M5：FastLIO 移植、真车联调、场测

## 六、下一步行动（按序）

1. TTS 播报/智能提示（M4 尾）
2. 调速 + Twist→Ecu 适配节点（仿真验证数值）
3. IMU 仿真插件 + EKF 融合（治 AMCL 发散 + 合同点名 + FastLIO 前置）
4. patrol_manager 真执行器 + 一键返回（仿真验收 3 点无人值守）
5. 并行催办：厂家导航源码、气体寄存器表、后轮转向逻辑、SIM 卡复测
