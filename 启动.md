当前启动命令速查：

## ① 全栈（仿真 + Nav2 + 监控页 + 视频 + 识别）——主用

```bash
bash ~/ros2Project/Remote_ctrol/scripts/start_nav2_monitor.sh
```

浏览器开 `http://localhost:8888/monitor.html`。要用 D435 的话先跑前置两条：

```powershell
# Windows PowerShell（每次 WSL 重启后一次）
usbipd attach --wsl --busid 1-19
```
```bash
# WSL（attach 后一次）
bash ~/ros2Project/Remote_ctrol/scripts/d435_attach_fix.sh
```

## ② 只看识别（演示视频，不开仿真/相机）

```bash
# 终端1：网关
ros2 run robot_gateway gateway
# 终端2：网页
python3 -m http.server 8888 --directory ~/ros2Project/Remote_ctrol/web
# 终端3：识别（吃测试视频）
ros2 run detector_node detector --ros-args -p video_source:=/home/ubuntu/ros2Project/test/test_patrol.mp4
```

## ③ 纯仿真遥控玩（无 Nav2、无识别）

```bash
bash ~/ros2Project/Remote_ctrol/scripts/start_sim_monitor.sh
```

注意：**新开终端已自动 source**（ROS2 环境 + 本工作区都写进 `~/.bashrc` 了），直接敲 `ros2 run` 就行。Ctrl-C 停脚本即全停。