# 巡检机器人远程控制与管理系统技术方案

> 版本：V1.0
> 适用场景：单台 ROS2 巡检机器人，4G 流量卡上网（贝锐蒲公英 R300S 工业路由器）
> 核心特点：基于蒲公英异地组网实现点对点通信，无需租用云服务器

---

## 一、项目背景与需求

本项目为单台 ROS2 巡检机器人的远程管理系统。机器人通过 R300S 工业路由器使用 4G 流量卡上网，处于运营商 CGNAT 网络之下，无公网 IP、无法端口映射。

### 1.1 功能需求清单

| 编号 | 需求 | 简述 |
|---|---|---|
| 1 | 远程操控 | 通过管理后台遥控界面远程操控机器人 |
| 2 | 手持遥控器 | 手持遥控器可遥控机器人行走 |
| 3 | 双向对讲 | 后台管理平台呼叫机器人本地，双向语音通话 |
| 4 | 语音播报 | 特定时段或特定区域进行音频文件播放 |
| 5 | 智能提示 | 针对自身运行状态的智能语音提醒 |
| 6 | 巡逻计划制定 | 巡检任务配置、路线制定、巡检点预置、计划制定、播报内容配置 |
| 7 | 自主巡逻 | 固定时间/固定周期无人值守自主巡逻，完成后生成巡检报告 |
| 8 | 一键返回 | 结束当前任务，一键返回出发点/充电桩 |
| 9 | 一键到达 | 一键到达地图任意点并执行该点位巡检工作 |
| 10 | 图片抓拍 | 自动或手动抓拍图片，自动保存 |
| 11 | 图像识别 | 巡检路线上识别人脸、行人、车牌，结果上报管理后台 |

### 1.2 方案选型结论

采用 **蒲公英异地组网（SD-WAN）+ 机器人自建服务的点对点架构**：

- 蒲公英组网将后台电脑与机器人纳入同一虚拟局域网，解决 4G CGNAT 无法直连的问题；
- 视频、对讲、遥控指令在虚拟局域网内点对点传输，无需公网中转服务器；
- 信令服务、流媒体服务、业务后端、数据库、管理后台网页全部部署在机器人本机；
- 不租用云服务器，不依赖 LiveKit Cloud 等付费服务。

---

## 二、总体架构

### 2.1 架构图

```
管理侧                            机器人现场
┌─────────────────┐               ┌──────────────────────────┐
│  管理后台电脑     │               │  R300S 工业路由器(4G)      │
│  ├─ 浏览器       │  蒲公英虚拟     │    │（LAN/WiFi）           │
│  │  (管理后台页面)│  局域网       │  机器人主控板(Jetson/x86)  │
│  │  视频/对讲/遥控)│◄═══════════►│    ├─ MediaMTX(流媒体)    │
│  └─ 蒲公英客户端  │  P2P直连/转发  │    ├─ 信令/网关服务        │
└─────────────────┘               │    ├─ 业务后端(FastAPI)   │
                                  │    ├─ SQLite 数据库       │
       手持遥控器                  │    ├─ 管理后台网页(静态托管)│
       (2.4G直插机器人)────────────►│    └─ ROS2 (Nav2/识别/音频)│
                                  └──────────────────────────┘
```

### 2.2 通信链路设计

| 数据类型 | 通道 | 协议 | 说明 |
|---|---|---|---|
| 实时视频 | 机器人 → 后台 | WebRTC（MediaMTX 拉流） | H.264，延迟 200–500ms，按需开启 |
| 双向对讲 | 双向 | WebRTC（后台经 WHIP 推麦克风上行） | 复用 MediaMTX |
| 遥控指令 | 后台 → 机器人 | WebSocket | 网关节点转 `/cmd_vel`，带心跳失控保护 |
| 遥测/状态/识别结果 | 机器人 → 后台 | WebSocket / HTTP 轮询 | 单台机器人无需 MQTT，简化架构 |
| 任务/计划下发 | 后台 → 机器人 | HTTP REST | 巡逻计划、点位、播报内容配置 |
| 图片/报告 | 机器人本机存储 | HTTP 下载 | 后台在线浏览或下载 |

> 说明：单台机器人场景下，后台与机器人是稳定的一对一连接，用 WebSocket + REST 即可满足全部数据交互，不再引入 MQTT  broker，减少部署组件。

### 2.3 部署位置

所有服务端组件（MediaMTX、网关、业务后端、数据库、后台前端页面）均部署于**机器人主控板**，浏览器通过蒲公英虚拟 IP 直接访问机器人，例如 `http://<机器人虚拟IP>:<端口>`。

---

## 三、蒲公英组网方案

### 3.1 组网组成

| 成员 | 类型 | 说明 |
|---|---|---|
| R300S 路由器 | 硬件成员 | 机器人侧，内置蒲公英组网，插 4G 流量卡即联网 |
| 管理后台电脑 | 软件成员 | 安装蒲公英访问端客户端 |
| （预留）手机/第二台电脑 | 软件成员 | 免费版共 3 个成员名额 |

### 3.2 关键限制与应对

1. **带宽**：蒲公英免费版 **P2P 直连不限速，服务器转发模式限速 2Mbps/节点**。1080p 视频约需 1.5–2Mbps，转发模式下仅能勉强传输，可能出现卡顿。
   - 应对一：部署后实测连接模式（蒲公英客户端可查看当前为 P2P 还是转发）；
   - 应对二：若长期为转发模式，视频降为 720p 并压低码率（1–1.5Mbps）；
   - 应对三：升级蒲公英付费版本提升转发带宽（年费数百元，仍低于云服务器方案）。
2. **成员数量**：免费版 3 个成员。多人/多终端同时查看需升级付费。
3. **可达性**：只有安装了蒲公英客户端并加入组网的终端才能访问管理后台。若甲方要求"任意浏览器随时访问"，需增加一台轻量云服务器运行后台（见第八章扩展路径）。

### 3.3 组网配置步骤

1. 蒲公英管理平台（pgy.oray.com）创建组网网络；
2. R300S 登录路由器管理页，绑定 Oray 账号并加入组网；
3. 管理电脑安装蒲公英访问端，登录同一账号加入组网；
4. 验证：管理电脑 ping 机器人主控板的虚拟 IP，确认连通；
5. 在蒲公英客户端确认当前链路为 P2P 模式（重点）。

---

## 四、机器人端软件设计

### 4.1 软件组件总览

| 组件 | 技术选型 | 职责 |
|---|---|---|
| 流媒体服务 | MediaMTX（单二进制，开源） | 摄像头 RTSP 汇聚，向浏览器提供 WebRTC 播放；接收浏览器 WHIP 推流（对讲上行） |
| 视频推流 | FFmpeg / GStreamer | 摄像头采集、H.264 编码（Jetson 走硬件编码 nvenc）推送本机 MediaMTX |
| 遥控网关 | Python (websockets) + ROS2 | 接收后台遥控指令转 `/cmd_vel`；心跳超时自动刹车 |
| 业务后端 | FastAPI + SQLite | 巡检计划 CRUD、任务调度、巡检报告生成、图片管理、识别记录存储 |
| 后台前端 | Vue/React 静态页面 | 由 FastAPI/Nginx 托管，浏览器访问 |
| ROS2 导航 | Nav2 + Waypoint Follower | 自主巡逻、一键到达、一键返回 |
| 语音模块 | TTS（edge-tts 在线 / 离线引擎）+ 音频播放节点 | 语音播报、智能提示 |
| 识别模块 | YOLOv8（人脸/行人）+ 车牌识别（PaddleOCR/LPRNet） | 边缘端推理，TensorRT 加速 |

### 4.2 ROS2 节点划分

```
robot_gateway_node        # WebSocket 网关：遥控指令 / 遥测上报
patrol_manager_node       # 巡逻任务管理：计划加载、调度执行、状态机
audio_player_node         # 音频播放：播报文件 / TTS 提示音
tts_alert_node            # 智能提示：监听电量/定位/急停/网络状态，触发 TTS
capture_server_node       # 图片抓拍：提供 ROS2 Service，本地保存+通知后端
detector_node             # 图像识别：人脸/行人/车牌推理，结果发布
intercom_node             # 对讲：接收 MediaMTX 上行音频并播放、回声消除
twist_mux (现成包)        # 指令仲裁：手持遥控器 > 远程遥控 > 自主导航
joy_node + teleop (现成包)# 手持遥控器
Nav2 (现成框架)           # navigate_to_pose / waypoint_follower
```

### 4.3 指令优先级与安全机制

- **仲裁优先级**（twist_mux）：手持遥控器（最高）> 远程遥控 > Nav2 自主导航；
- **失控保护**：远程遥控心跳超时 500ms 未收到指令即发布零速度；
- **速度限幅**：远程遥控模式下速度强制限制在安全阈值内；
- **急停**：后台急停按钮与机器人硬件急停均直接切断运动指令。

---

## 五、功能需求实现映射

### (1) 远程操控
浏览器加载机器人本机托管的后台页面 → MediaMTX WebRTC 拉流显示画面（200–500ms 延迟）→ 页面虚拟摇杆/键盘通过 WebSocket 发速度指令 → 网关节点转 `/cmd_vel` → twist_mux 仲裁输出。心跳超时自动刹车。

### (2) 手持遥控器
2.4G/USB 接收器直插机器人主控板 → `joy_node` + `teleop_twist_joy` 发布 `/cmd_vel`，twist_mux 中配置最高优先级，可随时接管远程遥控与自主导航。本地链路，不依赖网络。

### (3) 双向对讲
- 下行（机器人→后台）：机器人麦克风音频随摄像头一同推入 MediaMTX，浏览器播放；
- 上行（后台→机器人）：浏览器采集麦克风，通过 MediaMTX 的 WebRTC 推流（WHIP）上行，机器人端拉取该音频流播放；
- 机器人端需启用回声消除（AEC），可使用 WebRTC 音频处理库（如 speexdsp / webrtc-audio-processing）。

### (4) 语音播报
- 播报内容在后台配置（音频文件上传或 TTS 文本），随巡检计划下发；
- 触发方式两种：**定时触发**（任务调度器按时间播放）；**区域触发**（巡逻管理节点根据 Nav2 实时位姿判断是否进入预置电子围栏多边形，进入即播）；
- 播放由 `audio_player_node` 完成。

### (5) 智能提示
`tts_alert_node` 订阅以下状态并触发本地 TTS 播报，同时上报后台：
- 电量低于阈值；定位丢失/定位漂移；急停触发；网络断连与恢复；任务开始/完成/失败；传感器异常。

### (6) 巡逻计划制定
后台提供配置界面，数据存于机器人 SQLite：
- 巡检点预置：在地图上标注点位（记录 map 坐标系 x/y/yaw），支持为每个点位配置动作序列（停留时长、抓拍、播报内容、识别开关）；
- 巡检路线：巡检点有序集合；
- 巡检计划：路线 + 执行时间（固定时刻 / 固定周期，如每日 09:00、每 4 小时）；
- 语音播报内容配置：绑定到点位或路线。

### (7) 自主巡逻
- `patrol_manager_node` 内置调度器，按计划触发任务（任务缓存在本机，**断网也能正常执行**，无人值守的底线）；
- 执行采用 Nav2 Waypoint Follower，每个巡检点通过自定义插件执行动作序列（停留→抓拍→播报→识别）；
- 任务全程记录：时间轴、轨迹、各点位执行结果、抓拍图片、识别记录；
- 任务结束后由业务后端汇总生成**巡检报告**（HTML/PDF），后台可查看下载。

### (8) 一键返回
后台按钮 → REST 接口 → 调用 Nav2 `navigate_to_pose`，目标为预置出发点/充电桩坐标。如配置充电桩，可扩展自动对接回充（dock 检测 + 末端对位）。

### (9) 一键到达
后台地图上点击任意点（或选择预置点）→ 坐标下发 → `navigate_to_pose` 执行 → 到达后自动执行该点位预置的巡检动作序列。

### (10) 图片抓拍
- 手动：后台"抓拍"按钮 → `capture_server_node` 抓当前帧 → 本地存储（按日期/任务归档）→ 记录写入数据库，后台图片墙浏览；
- 自动：巡逻任务中由点位动作序列触发；识别到目标时也可联动抓拍留证。

### (11) 图像识别
- **边缘端推理**（Jetson 平台，TensorRT 加速），不上传视频到云端分析：
  - 人脸：YOLOv8 检测 + InsightFace 特征比对（预置人脸库）；
  - 行人：YOLOv8；
  - 车牌：车牌检测 + PaddleOCR/LPRNet 识别；
- 识别结果（类型、置信度、时间、巡检点位、裁剪小图）写入本机数据库并实时推送后台告警；
- 原图本机留存，后台按需调取。

---

## 六、部署清单

### 6.1 机器人端

| 类别 | 内容 |
|---|---|
| 硬件 | 主控板（建议 Jetson Orin Nano/NX 或同等 x86 工控机）、摄像头（带麦克风）、扬声器、R300S 路由器、手持遥控器接收器 |
| 系统 | Ubuntu 22.04 + ROS2 Humble |
| ROS2 包 | Nav2 全家桶、twist_mux、joy、teleop_twist_joy、image_transport |
| 第三方 | MediaMTX、FFmpeg/GStreamer（硬编码）、fastapi、uvicorn、websockets、SQLite、ONNX Runtime/TensorRT、YOLOv8、InsightFace、PaddleOCR |
| 自研节点 | 见 4.2 节 |

### 6.2 管理侧

| 类别 | 内容 |
|---|---|
| 硬件 | 普通办公电脑 |
| 软件 | 蒲公英访问端客户端、Chrome/Edge 浏览器（WebRTC 兼容） |

### 6.3 端口规划（机器人本机）

| 端口 | 用途 |
|---|---|
| 8554 | MediaMTX RTSP（本机推流） |
| 8889 | MediaMTX WebRTC 播放 |
| 8888 | 管理后台网页 + REST API |
| 8765 | WebSocket（遥控/遥测） |

> 以上端口均只绑定蒲公英虚拟网卡或本机回环，不暴露到 4G 公网侧。

---

## 七、验证与测试计划

| 阶段 | 内容 | 通过标准 |
|---|---|---|
| 1. 组网验证 | R300S 与电脑组网，互 ping，查看连接模式 | P2P 模式连通；若为转发则评估码率策略 |
| 2. 视频链路 | 摄像头 → MediaMTX → 浏览器 WebRTC 拉流 | 720p 流畅，延迟 < 500ms |
| 3. 遥控链路 | 虚拟摇杆 → WebSocket → /cmd_vel | 指令到运动响应 < 300ms，断线自动刹车 |
| 4. 对讲 | 双向语音通话 | 清晰无回声，延迟可接受 |
| 5. 自主巡逻 | 配置 3 点路线定时执行 | 全程无干预完成，生成巡检报告 |
| 6. 识别联动 | 路线中放置人脸/车牌目标 | 识别上报后台 + 抓拍留证 |
| 7. 断网韧性 | 巡逻中断开 4G | 任务继续执行，数据本地留存，复网后补报 |

---

## 八、风险与扩展路径

### 8.1 已知风险

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| 蒲公英免费版转发限速 2Mbps | 视频卡顿 | 实测 P2P 连通率；降码率；必要时升级付费版 |
| 免费版 3 成员上限 | 多终端查看受限 | 升级付费版 |
| 后台依赖蒲公英客户端 | 无法任意浏览器访问 | 见 8.2 混合架构 |
| 对讲自研工作量 | 开发周期 | 优先实现下行监听，上行对讲二期完善 |

### 8.2 扩展路径：混合架构（预留）

若后续出现以下任一需求：
- 甲方要求任意浏览器/手机随时随地访问后台；
- 机器人数量增加到多台；
- 需要数据集中存储、远程运维机器人系统；

则增加一台轻量云服务器（2核2G，约 ¥60–100/月）：业务后台、数据库迁移上云，云服务器作为蒲公英软件成员加入组网，视频与遥控仍走蒲公英虚拟局域网直达机器人。原有机器人端组件全部保留，平滑升级。

---

## 九、成本估算（首年）

| 项目 | 费用 |
|---|---|
| 蒲公英免费版组网 | ¥0（3 成员内） |
| 云服务器 | ¥0（本方案不需要） |
| 4G 流量卡 | 按套餐，约 ¥50–100/月 |
| 软件（全部开源） | ¥0 |
| **合计（不含机器人硬件）** | **约 ¥600–1200/年** |
